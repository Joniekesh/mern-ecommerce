import styled from "styled-components";

const Container = styled.div`
	max-width: 1200px;
	overflow: hidden;
	margin: auto;
	margin-top: 7rem;
	padding: 0 2rem;
`;

const Error = () => {
	return <Container>Error Page</Container>;
};

export default Error;
